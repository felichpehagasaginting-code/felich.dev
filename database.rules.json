{
  "rules": {
    "presence": {
      ".read": true,
      "$path": {
        "$sessionId": {
          ".read": true,
          ".write": "!data.exists() || !newData.exists()"
        }
      }
    }
  }
}